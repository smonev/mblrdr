/** @jsx React.DOM */
var React = require('react');

var FeedArticles = React.createClass({
    render: function() {
        return (
            <span>

            </span>
        );
    }
});

module.exports = FeedArticles;
