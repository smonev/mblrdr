/** @jsx React.DOM */
var React = require('react');

var FolderFeeds = React.createClass({
    render: function() {
        return (
            <ul>
            </ul>
        );
    }
});

module.exports = FolderFeeds;
